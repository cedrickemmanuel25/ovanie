@props(['paginator'])
<nav class="ops-list-pagination" aria-label="Pagination"><span>{{ $paginator->firstItem() ?? 0 }} – {{ $paginator->lastItem() ?? 0 }} sur {{ $paginator->total() }}</span><div>@if($paginator->onFirstPage())<button class="ops-button ops-button-small" disabled>Précédent</button>@else<a class="ops-button ops-button-small" href="{{ $paginator->previousPageUrl() }}">Précédent</a>@endif<span>{{ $paginator->currentPage() }} / {{ $paginator->lastPage() }}</span>@if($paginator->hasMorePages())<a class="ops-button ops-button-small" href="{{ $paginator->nextPageUrl() }}">Suivant <x-operations.icon name="next"/></a>@else<button class="ops-button ops-button-small" disabled>Suivant</button>@endif</div></nav>
