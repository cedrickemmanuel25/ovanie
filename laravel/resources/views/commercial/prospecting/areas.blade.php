@extends('layouts.staff')
@section('title','Ma mission de prospection | OVANIE')
@section('inline_styles')
.mission-hero{background:linear-gradient(135deg,#0e2a58,#174b86);color:#fff;border-radius:15px;padding:22px;margin-bottom:15px;box-shadow:0 14px 32px rgba(16,38,79,.12)}.mission-hero-top{display:flex;align-items:flex-start;justify-content:space-between;gap:18px}.mission-kicker{font-size:9px;text-transform:uppercase;letter-spacing:.11em;color:#a9c8ef;font-weight:850}.mission-hero h2{margin:6px 0 7px;font-size:23px;letter-spacing:-.035em}.mission-hero p{margin:0;color:#d4e2f4;font-size:10.5px;line-height:1.6}.mission-period{padding:8px 11px;border:1px solid rgba(255,255,255,.2);border-radius:999px;background:rgba(255,255,255,.08);font-size:9.5px;font-weight:800;white-space:nowrap}.mission-team{display:flex;align-items:center;gap:6px;margin-top:17px}.mission-avatar{width:31px;height:31px;border-radius:50%;display:grid;place-items:center;background:#fff;color:#153b6a;font-size:9px;font-weight:900;border:2px solid rgba(255,255,255,.35)}.mission-team-label{margin-left:6px;color:#d3e1f2;font-size:9.5px}.mission-progress{margin-top:17px}.mission-progress-head{display:flex;justify-content:space-between;color:#d7e6f8;font-size:9px;margin-bottom:7px}.mission-progress-track{height:9px;border-radius:999px;background:rgba(255,255,255,.16);overflow:hidden}.mission-progress-track i{display:block;height:100%;border-radius:999px;background:#f97316}.mission-kpis{grid-template-columns:repeat(4,minmax(0,1fr));margin-bottom:15px}.quarter-table .data-table{min-width:950px}.quarter-name{font-weight:850;color:#18365f}.quarter-meta{display:block;color:#8895a8;font-size:9px;margin-top:3px}.quarter-actions{display:flex;gap:7px;align-items:center}.quarter-state{display:inline-flex;min-height:24px;align-items:center;padding:0 9px;border-radius:999px;font-size:8.5px;font-weight:850;background:#eff3f7;color:#607089}.quarter-state.in_progress{background:#edf4ff;color:#0969d7}.quarter-state.completed{background:#eaf9f0;color:#087a48}.notice-box{border:1px solid #d9e4f0;background:#fff;border-radius:12px;padding:22px}.notice-box h2{margin:0 0 8px;font-size:16px}.notice-box p{margin:0;color:#697890;font-size:11px;line-height:1.65}.notice-box.warning{border-color:#fed7aa;background:#fffaf3}.notice-box.warning h2{color:#9a4e08}.next-mission{display:flex;justify-content:space-between;gap:16px;align-items:center;margin-top:14px;padding:13px 14px;border-radius:10px;background:#f6f9fd;border:1px solid #e1e8f0}.next-mission strong{font-size:11px;color:#193960}.next-mission span{display:block;margin-top:3px;color:#7c899d;font-size:9.5px}.history-list{display:grid;gap:9px}.history-item{display:grid;grid-template-columns:minmax(0,1fr) 120px 110px;gap:12px;align-items:center;border:1px solid #e5ebf2;border-radius:9px;padding:11px 12px}.history-item strong{font-size:10.5px;color:#1c3c65}.history-item span{display:block;color:#8591a3;font-size:9px;margin-top:3px}@media(max-width:850px){.mission-kpis{grid-template-columns:repeat(2,minmax(0,1fr))}.mission-hero-top{flex-direction:column}.history-item{grid-template-columns:1fr}}@media(max-width:560px){.mission-kpis{grid-template-columns:1fr}}
@endsection

@section('content')
<div class="page-header"><div><h1 class="page-title">Ma mission de prospection</h1><p class="page-subtitle">OVANIE vous affecte une commune et une période. Vous ne choisissez pas librement votre zone de prospection.</p></div><div class="page-actions"><a class="btn" href="{{ route('commercial.prospecting.prospects.index') }}"><i data-lucide="store"></i>Vendeurs prospectés</a>@if($currentMission)<a class="btn btn-orange" href="{{ route('commercial.prospecting.prospects.create',['mission_id'=>$currentMission->id]) }}"><i data-lucide="store-plus"></i>Ajouter un vendeur</a>@endif</div></div>

@if($currentMission)
    @php
        $progress=$currentMission->mission_quarters_count ? round(($currentMission->completed_quarters_count/$currentMission->mission_quarters_count)*100) : 0;
        $remaining=max(0,$currentMission->mission_quarters_count-$currentMission->completed_quarters_count);
    @endphp
    <section class="mission-hero">
        <div class="mission-hero-top"><div><div class="mission-kicker">Mission terrain affectée par OVANIE</div><h2>{{ $currentMission->commune?->name }}</h2><p>{{ $currentMission->instructions ?: 'Prospecter les vendeurs BTP de la commune, présenter OVANIE et accompagner les vendeurs intéressés jusqu’à l’ouverture de leur boutique.' }}</p></div><span class="mission-period">{{ $currentMission->starts_on?->format('d/m/Y') }} → {{ $currentMission->ends_on?->format('d/m/Y') }}</span></div>
        <div class="mission-team">@foreach($currentMission->members as $member)<span class="mission-avatar" title="{{ $member->name }}">{{ mb_strtoupper(mb_substr($member->name ?: 'C',0,1)) }}</span>@endforeach<span class="mission-team-label">{{ $currentMission->members->count() }} commercial(aux) travaillent ensemble sur cette commune.</span></div>
        <div class="mission-progress"><div class="mission-progress-head"><span>Couverture de la commune</span><strong>{{ $currentMission->completed_quarters_count }}/{{ $currentMission->mission_quarters_count }} quartiers · {{ $progress }}%</strong></div><div class="mission-progress-track"><i style="width:{{ $progress }}%"></i></div></div>
    </section>

    <section class="grid kpi-grid mission-kpis">
        <article class="kpi-card"><div class="kpi-head"><span class="kpi-label">Quartiers restant à couvrir</span><span class="kpi-icon"><i data-lucide="map-pinned"></i></span></div><div class="kpi-value">{{ $remaining }}</div><div class="kpi-foot">sur {{ $currentMission->mission_quarters_count }} quartiers de {{ $currentMission->commune?->name }}</div></article>
        <article class="kpi-card"><div class="kpi-head"><span class="kpi-label">Vendeurs identifiés</span><span class="kpi-icon"><i data-lucide="store"></i></span></div><div class="kpi-value">{{ $currentMission->prospects_count }}</div><div class="kpi-foot">partagés avec toute l’équipe de la mission</div></article>
        <article class="kpi-card"><div class="kpi-head"><span class="kpi-label">Vendeurs intéressés</span><span class="kpi-icon"><i data-lucide="handshake"></i></span></div><div class="kpi-value">{{ $currentMission->interested_count }}</div><div class="kpi-foot">à inscrire ou à relancer</div></article>
        <article class="kpi-card"><div class="kpi-head"><span class="kpi-label">Boutiques ouvertes</span><span class="kpi-icon"><i data-lucide="store-check"></i></span></div><div class="kpi-value">{{ $currentMission->shops_opened_count }}@if($currentMission->shop_target)/{{ $currentMission->shop_target }}@endif</div><div class="kpi-foot">issues de cette mission de prospection</div></article>
    </section>

    <section class="card quarter-table"><div class="card-head"><div><h2>Quartiers de {{ $currentMission->commune?->name }}</h2><p>Travaillez uniquement sur ces quartiers. L’avancement est partagé en temps réel avec les autres commerciaux de l’équipe.</p></div></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Quartier</th><th>État</th><th>Vendeurs trouvés</th><th>Intéressés</th><th>Boutiques ouvertes</th><th>Dernière action</th><th>Actions</th></tr></thead><tbody>
        @foreach($currentMission->missionQuarters->sortBy(fn($mq)=>$mq->quarter?->name) as $mq)
            @php $qs=$quarterStats->get($mq->quarter_id); $state=['pending'=>'À faire','in_progress'=>'En cours','completed'=>'Terminé'][$mq->status] ?? $mq->status; @endphp
            <tr><td><span class="quarter-name">{{ $mq->quarter?->name }}</span><span class="quarter-meta">{{ $currentMission->commune?->name }}</span></td><td><span class="quarter-state {{ $mq->status }}">{{ $state }}</span></td><td>{{ (int)($qs->prospects_count ?? 0) }}</td><td>{{ (int)($qs->interested_count ?? 0) }}</td><td>{{ (int)($qs->shops_count ?? 0) }}</td><td>@if($mq->completed_at){{ $mq->completed_at->format('d/m H:i') }}@elseif($mq->started_at){{ $mq->started_at->format('d/m H:i') }}@else—@endif<span class="record-sub">{{ $mq->updatedBy?->name ?: '' }}</span></td><td><div class="quarter-actions">
                <a class="btn" href="{{ route('commercial.prospecting.prospects.index',['mission_id'=>$currentMission->id,'quarter_id'=>$mq->quarter_id]) }}">Voir</a>
                @if($mq->status!=='completed')<a class="btn" href="{{ route('commercial.prospecting.prospects.create',['mission_id'=>$currentMission->id,'quarter_id'=>$mq->quarter_id]) }}">+ Vendeur</a>@endif
                @if($mq->status==='pending')<form method="POST" action="{{ route('commercial.prospecting.quarters.start',[$currentMission,$mq]) }}">@csrf<button class="btn btn-orange" type="submit">Commencer</button></form>@elseif($mq->status==='in_progress')<form method="POST" action="{{ route('commercial.prospecting.quarters.finish',[$currentMission,$mq]) }}">@csrf<button class="btn btn-primary" type="submit">Terminer</button></form>@endif
            </div></td></tr>
        @endforeach
    </tbody></table></div></section>
@else
    <section class="notice-box {{ $overdueMission ? 'warning' : '' }}"><h2>{{ $overdueMission ? 'Mission arrivée à échéance' : 'Aucune mission active aujourd’hui' }}</h2><p>@if($overdueMission)Votre mission à <strong>{{ $overdueMission->commune?->name }}</strong> s’est terminée le {{ $overdueMission->ends_on?->format('d/m/Y') }}. Vous ne pouvez pas continuer à prospecter cette commune tant que l’affectation n’est pas prolongée par OVANIE.@elseVous ne pouvez pas choisir vous-même une commune. Lorsqu’OVANIE vous affecte une mission, la commune, les quartiers, les dates et les membres de l’équipe apparaissent automatiquement ici.@endif</p>
        @if($upcomingMission)<div class="next-mission"><div><strong>Prochaine mission : {{ $upcomingMission->commune?->name }}</strong><span>{{ $upcomingMission->starts_on?->format('d/m/Y') }} → {{ $upcomingMission->ends_on?->format('d/m/Y') }} · équipe de {{ $upcomingMission->members->count() }} commercial(aux)</span></div><i data-lucide="calendar-days"></i></div>@endif
    </section>
@endif

@if($recentMissions->isNotEmpty())<section class="card" style="margin-top:15px"><div class="card-head"><div><h2>Missions terminées</h2><p>Historique récent de vos affectations de prospection.</p></div></div><div class="history-list">@foreach($recentMissions as $mission)<div class="history-item"><div><strong>{{ $mission->commune?->name }}</strong><span>{{ $mission->starts_on?->format('d/m/Y') }} → {{ $mission->ends_on?->format('d/m/Y') }}</span></div><div><strong>{{ $mission->prospects_count }} vendeur(s)</strong><span>{{ $mission->completed_quarters_count }}/{{ $mission->mission_quarters_count }} quartiers</span></div><div><strong>{{ $mission->shops_opened_count }} boutique(s)</strong><span>obtenues</span></div></div>@endforeach</div></section>@endif
@endsection
